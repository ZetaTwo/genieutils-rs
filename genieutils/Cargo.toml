[package]
name = "genieutils"
version = "0.1.0"
edition = "2021"

[dependencies]
binrw = "0.14.1"
flate2 = "1.0.35"
pyo3 = { version = "0.23.3", optional = true }
serde = { version = "1.0.216", optional = true, features = ["serde_derive"] }

[features]
serde = ["dep:serde"]
pyo3 = ["dep:pyo3"]
